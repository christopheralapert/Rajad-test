# Video assets

This folder is intended for optional hero/background videos (e.g. `hero.mp4`).

The original video was excluded from this GitHub-uploadable package to keep the repository small.
If you want the hero video on the site, add your own small `hero.mp4` here (or use Git LFS for large files).
